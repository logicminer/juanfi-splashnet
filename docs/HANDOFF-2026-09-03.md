# Production Handoff — 2026-09-03

State: **production-prepared**. All surfaces complete, full QA green
(40/40 API matrix + browser sweep + infra + outage drills), launch hygiene
done. This note is the operator-facing summary; runbooks live in
[LAUNCH-RUNBOOK.md](./LAUNCH-RUNBOOK.md) and [PILOT-KIT.md](./PILOT-KIT.md).

## What is live

| Surface | URL | Status |
|---|---|---|
| Admin Console (staff) | `splash.nxph.site/` | 10-section sidebar, approval gate, users, revenue |
| Advertiser Portal | `splash.nxph.site/portal` | builder + availability grid, charts, billing, account |
| Operator Console | `splash.nxph.site/operator` | income, self-serve site connect wizard, key rotation |
| Vendor quick-view | `splash.nxph.site/vendor` | public (API key) |
| Ad API / SDK | `/api/v1/*`, `/sdk/splash.js` | fail-open, self-updating |
| Assets CDN | `cdn.nxph.site` | MinIO-backed |
| JuanFi demo | `demo.nxph.site` + `vendo.nxph.site` | simulation only |
| Fork | github.com/logicminer/juanfi-splashnet | zero binaries |

## Credentials currently on the box — ROTATE AT HANDOFF

| Account | Purpose | Action |
|---|---|---|
| `cto@nxph.site` | platform ADMIN | change password (portal-style: any login → Account) or delete & recreate via /users |
| `marketing@islandcafe.ph` | advertiser demo (Island Cafe) | rotate or delete; keep only if used as demo |
| `ops@nextgenpiso.ph` | operator demo (NextGen) | rotate or delete |
| Vendor API keys | `vnk_…` for VND-3ADCCA | rotate from Operator Console → API Key |

No seeded/backdoor credentials exist; all were created through legitimate
flows and are listed here because they were set during development.

## Operational state

- 7 systemd units enabled (app, tunnel, pg, redis, minio, backup timer,
  health timer); crash-restart proven.
- Backups daily 03:00, 7-day retention (latest verified with full schema).
- Health probe every 5 min; set `ALERT_WEBHOOK` in `/etc/splashnet/health.env`
  for paging.
- QA is repeatable: `SPLASHNET_ADMIN_EMAIL=… SPLASHNET_ADMIN_PASSWORD=…
  [SPLASHNET_VENDOR_EMAIL=… …] python3 scripts/qa-matrix.py` (40 checks,
  self-sufficient fixtures, viewer-copy audit included).

## Known constraints (honest)

1. **Disk at 94% (13 GB free).** Comfortable for operations, but pruning
   personal data (models/Downloads, ~65 GB) is the owner's decision; the
   box cannot take another 97 GB-JuanFi-clone-scale surprise.
2. **Single host** (home ISP + one tunnel) — pilot-grade. Cloud migration
   is LAUNCH-RUNBOOK Phase 2 (env-only switch).
3. **Module C not built** — free-minutes vouchers need the connector (or the
   documented interim) for real hotspot sessions.
4. **No real router has run this yet** — PILOT-KIT is the gate to fleet.

## Go-live sequence (short form)

1. Rotate all credentials above; set `ALERT_WEBHOOK`.
2. Register the real operator (vendor + operator login), run their site
   through the connect wizard on one pilot router (PILOT-KIT tests P1–P9).
3. Sign the pilot advertiser (portal), approve their first campaign.
4. Review metrics daily (admin dashboard); run the QA matrix weekly.
5. On pilot success: cloud migration (Phase 2) → fleet rollout.
